const student = '/student'
const teacher = '/teacher'

const meva = '/meva'
const vocabulary = '/vocabulary'

export const paths = {
  student: {
    meva: student + meva,
    vocabulary: student + vocabulary,
  },
  teacher: {},
}
