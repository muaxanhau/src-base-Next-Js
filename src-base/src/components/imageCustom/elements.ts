import styled from 'styled-components'

export const Container = styled.div`
  overflow: hidden;

  &:hover img {
    scale: 1.1;
  }
`
export const Image = styled.img`
  transition: 0.2s;
  width: 100%;
`
